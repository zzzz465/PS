#include <stdio.h>
#define MAX_SIZE 10001

int connected[MAX_SIZE][MAX_SIZE];

int main() {
    int V, E;
    scanf("%d %d", &V, &E);

    for (int i = 0; i < E; i++) {
        int a, b, w;
        scanf("%d %d %d", &a, &b, &w);
        
    }

    return 0;
}